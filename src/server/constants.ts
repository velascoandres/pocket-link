const DEFAULT_PATH_SIZE = 6

const QUEUES = {
  LINK_QUEUE: 'LINK_QUEUE'
}


const JOB_DELAY_TIME = 5000

const JOB_CONCURRENCY = 5

const JOB_DELAY_REMOVE_TIME = 1000

const CONSTANTS = {
  JOB_DELAY_TIME,
  JOB_CONCURRENCY,
  JOB_DELAY_REMOVE_TIME,
  DEFAULT_PATH_SIZE,
  QUEUES,
} as const

export default CONSTANTS
